Отлично 💪
Сейчас мы сделаем идеальный **README.md** — чтобы твой проект на GitHub выглядел профессионально, был понятен преподавателю и сразу показывал, что работа выполнена на 100 %.

---

## 🧩 Шаг 1 — создаём файл

1. В IntelliJ в корне проекта (там же, где `pom.xml`)
   ➜ **Right click → New → File →** напиши имя

   ```
   README.md
   ```
2. Открой файл и вставь этот текст 👇

---

## ✅ Готовый README.md для твоего проекта

```markdown
# Assignment 4 — Graph Algorithms (Java / Maven)

Этот проект реализует основные алгоритмы работы с направленными графами на языке **Java 21**, включая:
- **Tarjan SCC** – поиск сильно связанных компонент  
- **Конденсация графа** – построение DAG на основе SCC  
- **Топологическая сортировка (Kahn)**  
- **Кратчайшие пути в DAG**  
- **Длиннейший путь (critical path)**  

---

## 🏗 Структура проекта
```

assignment4/
├── pom.xml                 # Maven-конфигурация (зависимости Gson и JUnit)
├── data/
│    └── tasks.json         # входной файл графа
└── src/
├── main/java/
│    ├── app/Main.java
│    ├── graph/
│    │     ├── model/Graph.java, Edge.java
│    │     ├── io/GraphIO.java
│    │     ├── scc/TarjanSCC.java
│    │     ├── topo/TopologicalSort.java
│    │     └── dagsp/DagShortestPaths.java, DagLongestPath.java
│    └── util/Metrics.java
└── test/java/graph/
├── SimpleTests.java
├── EdgeCasesTests.java
└── FromJsonDatasetTest.java

```

---

## ⚙️ Сборка и запуск

### Через IntelliJ IDEA
1. Открой `Main.java`
2. Нажми зелёный треугольник Run (main)
3. В **Run Configuration → Program arguments** укажи:
```

data/tasks.json

````

### Через Maven (при желании)
```bash
mvn -q -DskipTests package
mvn -q exec:java -Dexec.args="data/tasks.json"
````

---

## 🧠 Пример результата

```
SCC components:
C0 = [3, 2, 1]
C1 = [0]
C2 = [7]
C3 = [6]
C4 = [5]
C5 = [4]

Condensation DAG: n=6, edges=4  
Topological order: [1, 5, 0, 4, 3, 2]  
Shortest paths from component 5: dist = [∞, ∞, 8.0, 7.0, 2.0, 0.0]  
Critical path length = 8.0 → Path [5, 4, 3, 2]
```

---

## 🧪 Тестирование

Тесты реализованы на **JUnit 5** и включают:

* `SimpleTests` – проверка SCC и топосортировки
* `EdgeCasesTests` – граничные случаи (1 вершина, цепочка)
* `FromJsonDatasetTest` – полный путь от `tasks.json` до результатов

**Запуск:**
ПКМ по папке `test` → **Run Tests in 'graph'**

---

## 📊 Метрики и время работы

Класс `util.Metrics` собирает:

* количество обходов DFS и рёбер;
* push/pop операции в Kahn;
* количество релаксаций в DAG;
* время выполнения SCC в наносекундах.

---

## 💡 Зависимости

* **Gson 2.11.0** – чтение JSON
* **JUnit 5.10.0** – тестирование

---

## 👩‍💻 Автор

**Жанат Сагынбаева**
*Computer Science, Assignment 4, 2025*
GitHub: [yourusername](https://github.com/yourusername)

```

---

## 🧾 Шаг 2 — замени внизу ссылку
В блоке `👩‍💻 Автор` поменяй  
`yourusername` → **свой логин GitHub**.

---

## ✅ Шаг 3 — коммит
1. Выдели файл `README.md`  
2. Нажми **Git → Commit...**  
3. Напиши коротко:
```



